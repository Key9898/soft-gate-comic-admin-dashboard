# Soft-Gate Comic Admin environment variable templates

# Set to true to bypass backend verification in staging
VITE_USE_MOCK_API=true

# Empty VITE_API_BASE_URL uses the Vite /api proxy (same-origin). Split-host needs this URL plus API CORS_ORIGINS.
VITE_API_BASE_URL=http://localhost:3000
